apiVersion: v1
kind: Service
metadata:
  name: krakend-service
  namespace: krakend-whb
spec:
 type: NodePort
 ports:
 - name: http
   port: 8080
   targetPort: 8080
   protocol: TCP
 selector:
   app: krakend